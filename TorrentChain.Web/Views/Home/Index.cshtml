@using TorrentChain.Data.Utils;
@model TorrentChain.Web.Models.HomeViewModel;
<div>
    <form enctype="multipart/form-data" asp-controller="Home" asp-action="UploadTorrent">
        <input type="file" name="files" multiple />
        <input type="submit" value="Upload" />
    </form>

    <div id="root"></div>
    <link rel="stylesheet" href="@Url.Content("/dist/app.css")" asp-append-version="true" />
    <script src="@Url.Content("/dist/app.js")" asp-append-version="true"></script>

    <!-- <ul>
        @foreach (var item in Model.BlockChain)
        {
            <li>
                <div>
                    <strong>Index:</strong> <span>@item.Index</span>
                </div>
                <div>
                    <strong>Hash:</strong> <span>@Convert.ToBase64String(item.Hash.ToArray())</span>
                </div>
                <div>
                    <strong>Previous Hash:</strong> <span>@Convert.ToBase64String(item.PreviousHash.ToArray())</span>
                </div>
                <div>
                    <strong>Data:</strong> <span>@item.BlockData.Data.Count()</span>
                </div>
                <div>
                    <strong>Torrent Title:</strong> <span>@BlockUtils.GetTorrentTitle(item)</span>
                </div>
                <div>
                    <strong>Torrent Author:</strong> <span>@BlockUtils.GetTorrentAuthor(item)</span>
                </div>
                <div>
                    <strong>TimeStamp:</strong> <span>@item.TimeStamp.ToLocalTime()</span>
                </div>
            </li>
        }
    </ul> -->
</div>
