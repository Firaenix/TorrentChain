{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "command": "dotnet",
      "type": "process",
      "args": ["build", "${workspaceFolder}/TorrentChain.Web/TorrentChain.Web.csproj"],
      "problemMatcher": "$msCompile"
    }
  ]
}
